import { PageStruct } from "../types"

export const creteContactPage = (): PageStruct => {
    return {
        path: 'contact',
        title: "Contact Page",
        description: "I am a description edit me!",
    }
}
